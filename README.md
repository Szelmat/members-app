# Tagkezelő alkalmazás

## Backend

### Követelmények
* Python 3
* pip

### Szükséges modulok telepítése
```bash
$ cd backend
$ pip install pipenv
$ pipenv shell
$ pipenv install
```
### A szerver indítása
```bash
$ python manage.py runserver
```
